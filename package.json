{
  "name": "shipgraph-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "shipgraph — framework-agnostic, MIT graph visualization component. Monorepo root.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @shipgraph/core && npm run build -w @shipgraph/vue",
    "test": "npm run test -w @shipgraph/core && npm run test -w @shipgraph/vue"
  }
}
